# Full-building floor explosion plan

1. Replace per-floor selection with one building-level exploded state. Keep all floor nodes visible and equally emphasized, and animate them toward deterministic vertical offsets with a reduced-motion fast path.
2. Turn the floor list into a read-only section legend. Add a synchronized status treatment, staggered reveal, scanning spine, and pulse accents without changing the GLB hierarchy or campus layout.
3. Run focused tests, the full test/build suite, then verify the live interaction at port 5180 and capture a screenshot.
